<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!--定义seekbar滑动条的底色-->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="40dp" />
            <gradient
                android:centerColor="@color/seekbar_back"
                android:endColor="@color/seekbar_back"
                android:startColor="@color/seekbar_back" />
        </shape>
    </item>
    <!--定义seekbar滑动条进度颜色-->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="40dp" />
                <solid android:color="@color/seekbar_for" />
            </shape>
        </clip>
    </item>
</layer-list>